/**
 * Tradução de feedbacks movida para o Client Component FeedbackList.
 * A tradução acontece on-demand no browser via translate.googleapis.com —
 * sem API key, sem armazenamento, sem backend.
 *
 * Este arquivo é mantido apenas para não quebrar imports existentes durante
 * a transição. Pode ser removido quando todos os imports forem atualizados.
 */
export {};
